# Install
`yarn install`

# Setup
1. Go to [Plotly](https://chart-studio.plotly.com/Auth/login/#/) and create an account
2. Go to Profile Settings -> API Keys and get an api key
3. Add these credentials to the `.env` file

# Usage
`yarn start`
Process will continue to run in the background and auto reload every time changes are made
